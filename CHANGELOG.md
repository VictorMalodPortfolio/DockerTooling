# Changelog

## [Unreleased]

## [1.0.0] - 2026-03-29

### Added
- Dockerfile with kubectl, helm, tofu, k9s, stern, sops, age
- `docker-compose.yml` with kubeconfig, SSH keys and age key mounts
- Launch scripts for bash (`scripts/bash/tooling.sh`) and PowerShell (`scripts/powershell/tooling.ps1`)
- `.bashrc` with shell completions, aliases and kube-context prompt
